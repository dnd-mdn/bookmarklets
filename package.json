{
  "name": "bookmarklets",
  "version": "1.0.0",
  "private": true,
  "description": "Bookmarklet library viewer",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && node server.js",
    "test": "echo \"No tests configured\""
  }
}